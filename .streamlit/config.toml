[theme]
primaryColor = "#1f77b4"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"

[server]
headless = true
# Note: CORS and XSRF settings removed - Streamlit Cloud handles these internally
# Adding them can interfere with Streamlit Cloud's authentication

[browser]
gatherUsageStats = false
